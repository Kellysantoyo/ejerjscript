
let numero;
    numero=prompt("introducir un numero");
    if (numero %2==0) {
    document.write ("El numero" + numero + " es divisible entre dos");
    } else {
    document.write ("El numero" + numero + " No es divisible entre dos");
    }
